# Limitations

- Phase 05 artifacts currently present the smoke profile unless explicitly regenerated with the paper profile.
- The package validates saved outputs and schemas; it does not claim that smoke-profile uncertainty intervals are manuscript-ready.
- Model checkpoints and training outputs are treated as existing artifacts. PR 5 does not retrain or alter model metrics.
- Some narrative surfaces may still require later editorial cleanup; authoritative tables and paper-ready generated artifacts use saved CSV/JSON sources.
