# COSTTRACE Supplementary Package

This folder is the reviewer-facing index for supplementary materials. It does not duplicate every large artifact; instead it provides stable descriptions and paths to the authoritative CSV/JSON files generated by the reproducibility pipeline.

Use `supplement/supplement_manifest.csv` as the inventory. Runtime assumptions, limitations, and reproduction instructions are documented in adjacent Markdown files.
